extends layout

block css
  link(type='text/css' rel='stylesheet' href='/css/pages/user.css')

block js
  include includes/ga

block content
  include includes/components/header
  .content
    .user-bio
      .user-image
        a(href='http://twitter.com/'+user.username)
          img.img(src=user.twitterProfilePicture)
      h2.name= user.name
      h3.bio= user.bio
      h4.usernum User ##{user.uid}